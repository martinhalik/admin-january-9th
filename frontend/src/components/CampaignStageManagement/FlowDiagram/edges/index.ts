import ConnectionEdge from "./ConnectionEdge";
import StateEdge from "./StateEdge";
import PhaseEdge from "./PhaseEdge";

export const edgeTypes = {
  connection: ConnectionEdge,
  stateEdge: StateEdge,
  phaseEdge: PhaseEdge,
};

export { ConnectionEdge, StateEdge, PhaseEdge };

